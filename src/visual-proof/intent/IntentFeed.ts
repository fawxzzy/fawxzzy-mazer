// Visual-proof consumes shared planner truth through adapter facades only.
export {
  buildIntentFeed,
  resolveVisibleIntentEntries,
  resolveVisibleWorldPings
} from '../../mazer-core/intent/IntentFeed';
export type { IntentFeedBuildResult } from '../../mazer-core/intent/IntentFeed';
